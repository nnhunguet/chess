import React from 'react';
import {StyleSheet, View} from 'react-native';
import Background from './source/Background';
const App = () => {
  return (
    <View style={{flex: 1}}>
      <Background />
    </View>
  );
};

export default App;

const styles = StyleSheet.create({});
